// Kubernetes CKAD (Certified Kubernetes Application Developer) - 60 Questions
const CKAD_QUESTIONS = [
// ── Application Design and Build ──
{id:'ckad1',exams:['ckad'],domain:'Application Design and Build',text:'Which Kubernetes object is the smallest deployable unit that can be created and managed?',options:['Container','Pod','Deployment','ReplicaSet'],correct:1,explanation:'A Pod is the smallest deployable unit in Kubernetes. It wraps one or more containers that share network and storage resources.'},
{id:'ckad2',exams:['ckad'],domain:'Application Design and Build',text:'A pod needs an init container that runs a database migration before the main app starts. Which field in the pod spec defines init containers?',options:['containers','sidecarContainers','initContainers','preContainers'],correct:2,explanation:'initContainers run to completion in order before any app containers start. They are defined in the spec.initContainers field.'},
{id:'ckad3',exams:['ckad'],domain:'Application Design and Build',text:'What is the purpose of a multi-container pod with a sidecar pattern?',options:['To run multiple replicas of the same app','To add supporting functionality (logging, proxying) alongside the main container','To scale the pod horizontally','To replace the main container on failure'],correct:1,explanation:'The sidecar pattern places a helper container in the same pod as the main container. Common uses are log shipping, service mesh proxies, and config reloaders.'},
{id:'ckad4',exams:['ckad'],domain:'Application Design and Build',text:'A Dockerfile CMD instruction is overridden in a pod spec by which field?',options:['command','args','entrypoint','override'],correct:1,explanation:'In Kubernetes, "command" overrides the Docker ENTRYPOINT and "args" overrides the Docker CMD. To override CMD only, set spec.containers[].args.'},
{id:'ckad5',exams:['ckad'],domain:'Application Design and Build',text:'Which volume type mounts a directory from the host node\'s filesystem into a pod?',options:['emptyDir','configMap','hostPath','persistentVolumeClaim'],correct:2,explanation:'hostPath mounts a file or directory from the host node\'s filesystem. It is useful for accessing node-level resources but reduces pod portability.'},
{id:'ckad6',exams:['ckad'],domain:'Application Design and Build',text:'An emptyDir volume is created when a pod is assigned to a node. When is its data deleted?',options:['When any container in the pod restarts','When the pod is removed from the node','After 24 hours','When the node reboots'],correct:1,explanation:'emptyDir data exists as long as the Pod runs on that node. Containers can restart and still access the data, but it is permanently deleted when the Pod is removed.'},
{id:'ckad7',exams:['ckad'],domain:'Application Design and Build',text:'You need to build a container image as part of a CI pipeline. Which Dockerfile instruction sets the working directory for subsequent RUN, CMD, and COPY instructions?',options:['WORKDIR','DIR','CD','SETDIR'],correct:0,explanation:'WORKDIR sets the working directory inside the container. If the directory does not exist, it is created automatically.'},
{id:'ckad8',exams:['ckad'],domain:'Application Design and Build',text:'Which kubectl command creates a pod manifest imperatively without applying it to the cluster?',options:['kubectl run nginx --image=nginx','kubectl run nginx --image=nginx --dry-run=client -o yaml','kubectl create pod nginx --image=nginx','kubectl generate pod nginx'],correct:1,explanation:'--dry-run=client -o yaml outputs the manifest without sending it to the API server — essential for generating boilerplate YAML in the exam.'},
{id:'ckad9',exams:['ckad'],domain:'Application Design and Build',text:'A Job needs to run 5 completions with 2 running at a time. Which spec fields control this?',options:['replicas and maxSurge','completions and parallelism','count and concurrency','instances and parallel'],correct:1,explanation:'spec.completions sets the total number of successful completions required; spec.parallelism sets how many pods run concurrently.'},
{id:'ckad10',exams:['ckad'],domain:'Application Design and Build',text:'Which Kubernetes object runs a pod on every node (or a subset) in the cluster?',options:['Deployment','StatefulSet','DaemonSet','ReplicaSet'],correct:2,explanation:'A DaemonSet ensures that all (or selected) nodes run a copy of a pod — used for node monitoring agents, log collectors, and CNI plugins.'},
{id:'ckad11',exams:['ckad'],domain:'Application Design and Build',text:'What does a CronJob\'s spec.schedule field use to define timing?',options:['ISO 8601 duration strings','Cron expression (e.g. "*/5 * * * *")','Unix timestamps','Human-readable strings like "every 5 minutes"'],correct:1,explanation:'CronJob schedule uses standard cron syntax: minute, hour, day-of-month, month, day-of-week. For example "0 */6 * * *" runs every 6 hours.'},
{id:'ckad12',exams:['ckad'],domain:'Application Design and Build',text:'Which resource limit prevents a container from using more CPU than allocated?',options:['requests.cpu','limits.cpu','cpu.max','throttle.cpu'],correct:1,explanation:'limits.cpu is a hard cap — the container is CPU-throttled if it tries to exceed it. requests.cpu is used for scheduling decisions only.'},

// ── Application Deployment ──
{id:'ckad13',exams:['ckad'],domain:'Application Deployment',text:'What is the default deployment strategy in Kubernetes?',options:['Recreate','RollingUpdate','BlueGreen','Canary'],correct:1,explanation:'RollingUpdate is the default strategy. It gradually replaces old pods with new ones, maintaining availability. Recreate kills all old pods before creating new ones.'},
{id:'ckad14',exams:['ckad'],domain:'Application Deployment',text:'Which Deployment fields control rolling update speed?',options:['minReady and maxWait','maxUnavailable and maxSurge','minReplicas and maxReplicas','rolloutSpeed and batchSize'],correct:1,explanation:'maxUnavailable sets how many pods can be unavailable during the update; maxSurge sets how many extra pods can be created above the desired count.'},
{id:'ckad15',exams:['ckad'],domain:'Application Deployment',text:'How do you roll back a Deployment to the previous revision?',options:['kubectl rollout undo deployment/my-app','kubectl revert deployment/my-app','kubectl rollback deployment/my-app','kubectl apply --previous deployment/my-app'],correct:0,explanation:'kubectl rollout undo deployment/<name> rolls back to the previous revision. Add --to-revision=N to target a specific revision.'},
{id:'ckad16',exams:['ckad'],domain:'Application Deployment',text:'A Helm chart is being used to deploy an application. Which command installs the chart with a custom values file?',options:['helm deploy myapp ./chart -f values.yaml','helm install myapp ./chart -f values.yaml','helm apply myapp ./chart --values values.yaml','helm run myapp ./chart -f values.yaml'],correct:1,explanation:'helm install <release> <chart> -f <values-file> installs a new release using the specified values override file.'},
{id:'ckad17',exams:['ckad'],domain:'Application Deployment',text:'Which kubectl command scales a Deployment to 5 replicas?',options:['kubectl resize deployment/my-app --replicas=5','kubectl scale deployment/my-app --replicas=5','kubectl set replicas deployment/my-app 5','kubectl update deployment/my-app --replicas=5'],correct:1,explanation:'kubectl scale deployment/<name> --replicas=<n> imperatively changes the replica count immediately.'},
{id:'ckad18',exams:['ckad'],domain:'Application Deployment',text:'What is a StatefulSet used for compared to a Deployment?',options:['StatefulSets use more resources','StatefulSets provide stable network IDs, stable storage, and ordered pod management for stateful apps','StatefulSets are faster to deploy','StatefulSets automatically scale based on load'],correct:1,explanation:'StatefulSets give each pod a stable hostname (pod-0, pod-1), a dedicated PersistentVolume, and guarantee ordered startup/shutdown — required for databases.'},
{id:'ckad19',exams:['ckad'],domain:'Application Deployment',text:'How do you update only the image of a container in a running Deployment without editing the full manifest?',options:['kubectl update deployment/my-app --image=nginx:1.25','kubectl set image deployment/my-app nginx=nginx:1.25','kubectl patch deployment my-app -p \'{"image":"nginx:1.25"}\'','kubectl replace deployment my-app --image=nginx:1.25'],correct:1,explanation:'kubectl set image deployment/<name> <container>=<image>:<tag> patches just the image field, triggering a rolling update.'},
{id:'ckad20',exams:['ckad'],domain:'Application Deployment',text:'What does kubectl rollout status deployment/my-app report?',options:['The current CPU usage of the deployment','Whether the rollout has completed successfully or is still in progress','The deployment\'s YAML manifest','The history of all rollouts'],correct:1,explanation:'kubectl rollout status waits and streams the progress of a rollout, exiting 0 on success — useful in CI/CD pipelines to gate on deployment completion.'},

// ── Application Observability and Maintenance ──
{id:'ckad21',exams:['ckad'],domain:'Application Observability and Maintenance',text:'Which probe checks whether a container is alive and restarts it if it fails?',options:['readinessProbe','startupProbe','livenessProbe','healthProbe'],correct:2,explanation:'livenessProbe detects deadlocks or unresponsive containers and triggers a restart. readinessProbe controls traffic routing. startupProbe protects slow-starting apps.'},
{id:'ckad22',exams:['ckad'],domain:'Application Observability and Maintenance',text:'A readinessProbe fails on a pod. What happens to traffic for that pod?',options:['The pod is restarted','The pod is removed from Service endpoints and receives no traffic','The pod is deleted','Nothing — traffic continues as normal'],correct:1,explanation:'When a readinessProbe fails the pod is removed from the Service\'s endpoint list. It stays running but receives no traffic until probes pass again.'},
{id:'ckad23',exams:['ckad'],domain:'Application Observability and Maintenance',text:'Which kubectl command streams the live logs of a running container?',options:['kubectl logs my-pod --watch','kubectl logs my-pod -f','kubectl tail my-pod','kubectl stream logs my-pod'],correct:1,explanation:'kubectl logs <pod> -f (--follow) streams logs in real time. Add -c <container> to specify a container in a multi-container pod.'},
{id:'ckad24',exams:['ckad'],domain:'Application Observability and Maintenance',text:'How do you get a shell inside a running container for debugging?',options:['kubectl ssh my-pod','kubectl exec -it my-pod -- /bin/sh','kubectl attach my-pod --shell','kubectl debug my-pod --shell'],correct:1,explanation:'kubectl exec -it <pod> -- <command> opens an interactive terminal. Use /bin/bash or /bin/sh depending on the container image.'},
{id:'ckad25',exams:['ckad'],domain:'Application Observability and Maintenance',text:'What does the startupProbe do that livenessProbe alone cannot?',options:['It replaces readinessProbe for startup traffic','It gives slow-starting containers extra time before liveness checks begin, preventing premature restarts','It checks the container image digest','It validates environment variables at startup'],correct:1,explanation:'startupProbe disables liveness and readiness probes until it succeeds, giving legacy apps or slow initializers time to boot without being killed.'},
{id:'ckad26',exams:['ckad'],domain:'Application Observability and Maintenance',text:'Which command shows resource usage (CPU/memory) for pods in the current namespace?',options:['kubectl describe pods','kubectl top pods','kubectl stats pods','kubectl usage pods'],correct:1,explanation:'kubectl top pods (requires metrics-server) shows live CPU and memory consumption per pod — useful for right-sizing resource requests and limits.'},
{id:'ckad27',exams:['ckad'],domain:'Application Observability and Maintenance',text:'A pod is in CrashLoopBackOff. What is the first kubectl command to diagnose the issue?',options:['kubectl delete pod my-pod','kubectl logs my-pod --previous','kubectl describe node','kubectl rollout restart deployment/my-app'],correct:1,explanation:'kubectl logs <pod> --previous shows logs from the last crashed container instance, revealing why it exited.'},
{id:'ckad28',exams:['ckad'],domain:'Application Observability and Maintenance',text:'Which field in a pod spec sets the container\'s restart policy?',options:['spec.containers[].restartPolicy','spec.restartPolicy','spec.containers[].lifecycle','metadata.restartPolicy'],correct:1,explanation:'spec.restartPolicy applies to all containers in the pod. Values are Always (default), OnFailure, and Never.'},

// ── Application Environment, Configuration and Security ──
{id:'ckad29',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'What is the difference between a ConfigMap and a Secret?',options:['ConfigMaps hold more data','Secrets are base64-encoded and intended for sensitive data; ConfigMaps are for non-sensitive configuration','Secrets are encrypted at rest by default; ConfigMaps are not','There is no difference'],correct:1,explanation:'Secrets are base64-encoded (not encrypted by default unless ETCD encryption is enabled) and intended for sensitive data. ConfigMaps are plain key-value stores for configuration.'},
{id:'ckad30',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'How do you expose all keys of a ConfigMap as environment variables in a pod?',options:['Use envFrom with configMapRef','Use env with configMapKeyRef for each key','Mount the ConfigMap as a volume','Use valueFrom.configMapRef'],correct:0,explanation:'envFrom: [{configMapRef: {name: my-cm}}] injects all keys from the ConfigMap as environment variables without listing each one individually.'},
{id:'ckad31',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'A pod needs to read a Secret value as a file. Which approach mounts the Secret as a volume?',options:['spec.env[].valueFrom.secretKeyRef','spec.volumes[].secret.secretName + volumeMounts','spec.envFrom[].secretRef','spec.containers[].secretMount'],correct:1,explanation:'Define a volume with secret.secretName and mount it with volumeMounts. Each key becomes a file in the mount path, readable by the container.'},
{id:'ckad32',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'What does a ServiceAccount provide to a pod?',options:['Network access to external services','An identity for processes in the pod to authenticate with the Kubernetes API','Root access to the node','Automatic TLS certificates for the pod'],correct:1,explanation:'A ServiceAccount gives pods an identity. The API server uses it for RBAC authorization — controlling what API resources the pod\'s processes can access.'},
{id:'ckad33',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'Which SecurityContext field prevents a container from running as root?',options:['allowPrivilegeEscalation: false','runAsNonRoot: true','privileged: false','readOnlyRootFilesystem: true'],correct:1,explanation:'runAsNonRoot: true causes the kubelet to validate that the container does not start as UID 0 (root), failing the pod if it tries to.'},
{id:'ckad34',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'What does setting readOnlyRootFilesystem: true in a SecurityContext do?',options:['Prevents the container from reading any files','Mounts the container\'s root filesystem as read-only, preventing file system writes','Makes the container image read-only','Prevents reading secrets from the filesystem'],correct:1,explanation:'readOnlyRootFilesystem: true is a security best practice that prevents attackers from writing malicious files to the container filesystem at runtime.'},
{id:'ckad35',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'A ResourceQuota is applied to a namespace. What happens when a pod is created without resource requests?',options:['The pod is created with default requests from a LimitRange','The pod is rejected if the namespace ResourceQuota requires requests to be set','The pod runs without limits','The pod uses node default resources'],correct:1,explanation:'If a ResourceQuota specifies requests.cpu or requests.memory, every pod in that namespace must declare resource requests or the API server rejects it.'},
{id:'ckad36',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'What does a LimitRange define in a namespace?',options:['The maximum number of pods allowed','Default and maximum resource requests/limits for containers and pods','Network bandwidth limits','Storage quotas per PVC'],correct:1,explanation:'LimitRange sets default, min, and max CPU/memory requests and limits for containers, pods, and PVCs in a namespace — preventing resource abuse and missing requests.'},
{id:'ckad37',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'Which kubectl command creates a generic Secret from literal values?',options:['kubectl create secret generic db-pass --from-literal=password=s3cr3t','kubectl create secret db-pass --value=password=s3cr3t','kubectl apply secret generic db-pass --data=password=s3cr3t','kubectl new secret db-pass --literal=password=s3cr3t'],correct:0,explanation:'kubectl create secret generic <name> --from-literal=<key>=<value> is the imperative way. Multiple --from-literal flags can be used for multiple keys.'},
{id:'ckad38',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'What is the purpose of setting capabilities in a container SecurityContext?',options:['To grant the container extra network bandwidth','To add or drop Linux kernel capabilities (e.g. NET_ADMIN) beyond the default set','To enable GPU access','To control CPU scheduling priority'],correct:1,explanation:'securityContext.capabilities.add/drop lets you fine-tune Linux capabilities. Dropping ALL and adding only needed ones follows the principle of least privilege.'},

// ── Services & Networking ──
{id:'ckad39',exams:['ckad'],domain:'Services and Networking',text:'What is the difference between ClusterIP and NodePort Service types?',options:['ClusterIP exposes the service externally; NodePort is internal only','ClusterIP is accessible only within the cluster; NodePort additionally exposes the service on a static port on every node','NodePort is faster than ClusterIP','ClusterIP requires a load balancer'],correct:1,explanation:'ClusterIP is the default — internal only. NodePort opens a port (30000-32767) on every node, making the service reachable from outside the cluster without a load balancer.'},
{id:'ckad40',exams:['ckad'],domain:'Services and Networking',text:'How does a Service select which pods to route traffic to?',options:['By pod name','By label selectors matching pod labels','By pod IP address','By namespace only'],correct:1,explanation:'A Service uses spec.selector to match pod labels. Any pod with matching labels becomes an endpoint — enabling dynamic routing as pods are created or deleted.'},
{id:'ckad41',exams:['ckad'],domain:'Services and Networking',text:'What DNS name resolves to a Service named "api" in namespace "prod"?',options:['api.prod','api.prod.svc','api.prod.svc.cluster.local','prod.api.cluster.local'],correct:2,explanation:'Kubernetes DNS follows the pattern <service>.<namespace>.svc.cluster.local. Short names like "api" work within the same namespace; cross-namespace needs at least api.prod.'},
{id:'ckad42',exams:['ckad'],domain:'Services and Networking',text:'What does an Ingress resource do?',options:['Manages egress traffic from pods','Provides HTTP/HTTPS routing rules from outside the cluster to internal Services','Replaces the need for Services','Controls pod-to-pod communication'],correct:1,explanation:'An Ingress defines HTTP routing rules (host/path → Service) and exposes multiple services under one external IP via an Ingress Controller (nginx, traefik, etc.).'},
{id:'ckad43',exams:['ckad'],domain:'Services and Networking',text:'A NetworkPolicy is applied to pods with label app=backend, denying all ingress. How can the frontend pods communicate with backend pods?',options:['NetworkPolicies cannot block pod communication','Add a NetworkPolicy rule allowing ingress from pods with label app=frontend','Delete the NetworkPolicy','Use a Service instead'],correct:1,explanation:'You add an ingress rule to the backend NetworkPolicy with from.podSelector matching app=frontend. Without an explicit allow rule, all ingress is denied.'},
{id:'ckad44',exams:['ckad'],domain:'Services and Networking',text:'Which Service type provisions an external load balancer from the cloud provider?',options:['NodePort','ExternalName','ClusterIP','LoadBalancer'],correct:3,explanation:'LoadBalancer type triggers the cloud provider to provision an external load balancer (e.g. AWS ELB, GCP LB) that forwards traffic to NodePorts on cluster nodes.'},
{id:'ckad45',exams:['ckad'],domain:'Services and Networking',text:'What is a headless Service (clusterIP: None) used for?',options:['A Service with no selector','A Service that returns DNS A records for each pod IP instead of a single ClusterIP — used by StatefulSets for stable pod DNS','A disabled Service','A Service only accessible from localhost'],correct:1,explanation:'Headless Services allow clients to discover individual pod IPs via DNS. StatefulSets use them so each pod gets a stable DNS name like pod-0.svc.namespace.svc.cluster.local.'},
{id:'ckad46',exams:['ckad'],domain:'Services and Networking',text:'Which kubectl command exposes a Deployment as a ClusterIP Service on port 80 targeting container port 8080?',options:['kubectl expose deployment my-app --port=80 --target-port=8080','kubectl service deployment my-app --port=80:8080','kubectl create service my-app --from=deployment --port=80','kubectl bind deployment my-app --port=80 --container-port=8080'],correct:0,explanation:'kubectl expose deployment <name> --port=<service-port> --target-port=<container-port> creates a matching Service. Default type is ClusterIP.'},

// ── Mixed / Advanced ──
{id:'ckad47',exams:['ckad'],domain:'Application Design and Build',text:'Which field in a pod spec defines resource requests and limits for a container?',options:['spec.containers[].resources','spec.resources','spec.limits','spec.containers[].quota'],correct:0,explanation:'spec.containers[].resources.requests sets the minimum guaranteed resources; spec.containers[].resources.limits sets the maximum allowed. Both CPU and memory can be specified.'},
{id:'ckad48',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'What does the --record flag do when used with kubectl apply or kubectl set image?',options:['Records the command in the deployment rollout history (deprecated)','Saves the manifest to a local file','Records the event in CloudWatch','Enables audit logging'],correct:0,explanation:'--record (deprecated in newer versions) annotated the object with the kubectl command used, visible in rollout history. The annotation kubernetes.io/change-cause is now preferred.'},
{id:'ckad49',exams:['ckad'],domain:'Application Deployment',text:'A Deployment has 3 replicas. maxUnavailable=1 and maxSurge=1. During a rolling update, what is the maximum number of pods that can be running?',options:['3','4','5','2'],correct:1,explanation:'maxSurge=1 allows 1 extra pod above the desired 3, so up to 4 pods can run simultaneously during the update.'},
{id:'ckad50',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'You need a pod to run with a specific user ID. Which SecurityContext field sets this?',options:['runAsUser','userId','setUID','containerUser'],correct:0,explanation:'securityContext.runAsUser: <UID> sets the UID that container processes run as. It can be set at pod level (applies to all containers) or per container.'},
{id:'ckad51',exams:['ckad'],domain:'Application Observability and Maintenance',text:'What is the purpose of the kubectl describe command?',options:['Shows raw YAML of a resource','Shows detailed human-readable info including events, conditions, and related resources','Deletes a resource','Applies a resource manifest'],correct:1,explanation:'kubectl describe provides a human-readable summary with events, conditions, labels, and status — more useful than raw YAML for troubleshooting.'},
{id:'ckad52',exams:['ckad'],domain:'Services and Networking',text:'What happens to existing connections when a pod is removed from a Service\'s endpoints due to a failed readinessProbe?',options:['All connections are immediately terminated','New connections are not routed to the pod; existing connections may continue until they close','The pod is restarted','The Service is deleted'],correct:1,explanation:'Removing a pod from endpoints stops new connections from being sent to it. Existing long-lived connections (e.g. gRPC streams) are unaffected until the client reconnects.'},
{id:'ckad53',exams:['ckad'],domain:'Application Design and Build',text:'A pod must access a PersistentVolume for durable storage. What object binds a pod to a PersistentVolume?',options:['VolumeMount','StorageClass','PersistentVolumeClaim','VolumeBinding'],correct:2,explanation:'A PersistentVolumeClaim (PVC) is a request for storage. The pod references the PVC; Kubernetes binds it to a matching PersistentVolume.'},
{id:'ckad54',exams:['ckad'],domain:'Application Deployment',text:'Which Helm command upgrades a release with a new chart version?',options:['helm update myapp ./chart','helm upgrade myapp ./chart','helm apply myapp ./chart','helm rollout myapp ./chart'],correct:1,explanation:'helm upgrade <release> <chart> upgrades an existing release. Use --install to also install if the release does not exist yet.'},
{id:'ckad55',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'A pod needs access to the Kubernetes API. Which resource grants specific API permissions to a ServiceAccount?',options:['ClusterRole only','RoleBinding or ClusterRoleBinding linking a Role/ClusterRole to the ServiceAccount','NetworkPolicy','ResourceQuota'],correct:1,explanation:'RBAC: a Role (namespace-scoped) or ClusterRole defines permissions; a RoleBinding or ClusterRoleBinding grants those permissions to a ServiceAccount.'},
{id:'ckad56',exams:['ckad'],domain:'Application Design and Build',text:'What is the difference between a Job and a CronJob?',options:['Jobs run forever; CronJobs run once','A Job runs to completion once; a CronJob creates Jobs on a schedule','CronJobs are faster than Jobs','Jobs can only run one pod; CronJobs run many'],correct:1,explanation:'A Job ensures a pod runs to successful completion. A CronJob is a controller that creates a new Job on a cron schedule.'},
{id:'ckad57',exams:['ckad'],domain:'Services and Networking',text:'An Ingress rule routes traffic for host "api.example.com" to service "api-svc" on port 80. What additional component must exist in the cluster for Ingress to work?',options:['A LoadBalancer Service','An Ingress Controller (e.g. nginx-ingress)','A NodePort Service','A Gateway API'],correct:1,explanation:'An Ingress resource is just configuration. An Ingress Controller (a running pod that watches Ingress objects) is required to actually implement the routing rules.'},
{id:'ckad58',exams:['ckad'],domain:'Application Observability and Maintenance',text:'How do you view the events associated with a specific pod?',options:['kubectl events pod/my-pod','kubectl describe pod my-pod (Events section)','kubectl logs my-pod --events','kubectl get events --type=pod'],correct:1,explanation:'kubectl describe pod <name> shows a human-readable summary ending with Events — listing scheduling decisions, image pulls, probe failures, and restarts.'},
{id:'ckad59',exams:['ckad'],domain:'Application Environment, Configuration and Security',text:'Which admission controller automatically injects default resource limits from a LimitRange into pods that do not specify them?',options:['ResourceQuota','LimitRanger','PodSecurityAdmission','DefaultResourceLimits'],correct:1,explanation:'The LimitRanger admission controller reads LimitRange objects and injects default requests/limits into pods and containers that omit them.'},
{id:'ckad60',exams:['ckad'],domain:'Application Design and Build',text:'Which kubectl flag forces a resource to be replaced (delete + create) rather than patched in place?',options:['kubectl apply --force','kubectl replace --force','kubectl patch --replace','kubectl update --recreate'],correct:1,explanation:'kubectl replace --force deletes and recreates the resource. Use it when a field is immutable and cannot be patched — otherwise prefer kubectl apply.'},
];
